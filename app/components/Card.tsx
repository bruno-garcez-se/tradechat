  className={`bg-white rounded-lg shadow-lg p-6 ${
    variant === 'primary'
      ? 'border-2 border-[#000000]'
      : variant === 'secondary'
      ? 'border-2 border-[#00FF00]'
      : ''
  } ${className}`} 